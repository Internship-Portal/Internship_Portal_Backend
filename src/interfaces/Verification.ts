// --------------------------- verification Interface

export interface verification {
  email_id: string;
  otp: string;
  otpverified: boolean;
  expiresAt: Date;
}

// --------------------------- verification Interface
